apiVersion: s3.aws.crossplane.io/v1beta1
kind: Bucket
metadata:
  name: customer-crossplane-bucket
spec:
  forProvider:
    locationConstraint: us-east-1
  providerConfigRef:
    name: crossplane-system
